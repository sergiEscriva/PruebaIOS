name: Vista direcorio de trabajo

on:
  workflow_dispatch:
  push:
    branches:
      - main
  pull_request:

jobs:
  build-ios:
   runs-on: macos-latest
   steps:
      - name: Checkout repository
        uses: actions/checkout@v3

      - name: donde estoy?
        run: |
          ls
          pwd

      - name: Entrar en iosApp y ver contenido
        run: |
          cd iosApp
          pwd
          ls -la
